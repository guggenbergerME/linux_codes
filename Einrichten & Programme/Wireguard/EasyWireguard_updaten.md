# Update von EasyWireguard

Das Update auf die aktuelle Version erfolgt mit nachfolgendem Befehl
```
docker stop [ID des wg-easy Containers]
```
```
docker rm [ID des wg-easy Containers]
```
```
docker pull ghcr.io/wg-easy/wg-easy
```
